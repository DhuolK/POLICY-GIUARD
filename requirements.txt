flask==3.1.0
flask-login==0.6.3
flask-wtf==1.3.0
SQLAlchemy==2.0.37
Flask-SQLAlchemy==3.1.1
PyMySQL==1.1.1
cryptography==42.0.5
pymongo==4.9.0
python-dotenv==1.0.1
werkzeug==3.1.3
requests==2.32.3

# Request throttling on credential endpoints (login). Counters live in the
# app's MongoDB, so `limits` is a direct dependency: app/extensions.py and
# create_app() configure its MongoDBStorage backend explicitly (no Redis on
# shared hosting).
flask-limiter==4.1.1
limits==5.0.0

# Required by pymongo for mongodb+srv:// URIs (Atlas / production MONGO_URI)
dnspython==2.6.1