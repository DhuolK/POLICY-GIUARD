# Pinned from the known-good local virtualenv (pip freeze) so the server gets
# exactly the versions the app is developed/tested against.
#
# Install ON THE SERVER into the cPanel virtualenv (path shown in
# cPanel → Setup Python App), e.g.:
#   cd ~/policyguard
#   ~/virtualenv/policyguard/3.11/bin/pip install -r requirements.txt
#
# colorama/pywin32 are Windows-only local dev deps; the environment markers
# keep Linux installs clean without a separate file.
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.4.2
colorama==0.4.6 ; sys_platform == "win32"
Deprecated==1.3.1
dnspython==2.8.0
Flask==3.1.3
Flask-Limiter==4.1.1
Flask-Login==0.6.3
Flask-WTF==1.3.0
idna==3.19
iniconfig==2.3.0
itsdangerous==2.2.0
Jinja2==3.1.6
limits==5.8.0
MarkupSafe==3.0.3
ordered-set==4.1.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pymongo==4.17.0
pytest==9.1.1
python-dotenv==1.2.2
pywin32==312 ; sys_platform == "win32"
requests==2.34.2
typing_extensions==4.16.0
urllib3==2.7.0
Werkzeug==3.1.8
wrapt==2.4.1
WTForms==3.2.2
# Linux-only production WSGI server (nginx → gunicorn on the Oracle VM).
# Marker keeps Windows dev installs clean, same as colorama/pywin32 above.
gunicorn==26.2.0 ; sys_platform != "win32"
